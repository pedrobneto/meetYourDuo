import React from "react";
import {
    View,
    Text,
} from "react-native";

import styles from './styles'

const Feed = (props) => (
    <View style={styles.container}>
        <Text>Feed</Text>
    </View>
)
export default Feed;